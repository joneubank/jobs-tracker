{
  "name": "@joneubank/jobs-tracker-client",
  "version": "1.0.0",
  "description": "Library to send Job Status updates to Job Tracker server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && NODE_PATH=./src tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.23",
    "typescript": "~4.6.3"
  },
  "dependencies": {
    "logger": "workspace:*",
    "data-model": "workspace:*",
    "kafkajs": "~1.16.0",
    "optionals": "workspace:*"
  }
}
